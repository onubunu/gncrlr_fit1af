<%= form_for(@netz) do |f| %>
  <% if @netz.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@netz.errors.count, "error") %> prohibited this netz from being saved:</h2>

      <ul>
      <% @netz.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :n_name %><br />
    <%= f.text_field :n_name %>
  </div>
  <div class="field">
    <%= f.label :n_comment %><br />
    <%= f.text_area :n_comment %>
  </div>
  <div class="field">
    <%= f.label :n_logo_url %><br />
    <%= f.text_field :n_logo_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
